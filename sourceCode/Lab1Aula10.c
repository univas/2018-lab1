#include<stdio.h>

int main() {
	
	//mostrar os números de zero a dez
	int contador = 0;
	
	while(contador <= 10) {
		printf("Valor: %i\n", contador);
		
		contador++; //é a mesma coisa que: contador = contador + 1
	}
	
}
